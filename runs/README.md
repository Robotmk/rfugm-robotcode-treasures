# Recorded test runs

Two runs of the same test suite, recorded for chapter 8:

- `before/output.xml` — yesterday's nightly run
- `after/output.xml` — today's nightly run, after a change to `bank/core.py`

Only `output.xml` is kept — everything `robotcode results` needs.
